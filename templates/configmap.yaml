apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "my-app-chart.fullname" . }}-fluent-bit-config
data:
  fluent-bit.conf: |
    [INPUT]
    {{- toYaml .Values.fluentBit.config.input | nindent 8 }}

    [OUTPUT]
    {{- toYaml .Values.fluentBit.config.output | nindent 8 }}
